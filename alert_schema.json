{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Alert Event",
  "description": "Schema for vision system detection alerts",
  "type": "object",
  "properties": {
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 formatted timestamp of the detection"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "The YOLO model confidence score"
    },
    "camera_ID": {
      "type": "string",
      "description": "Unique identifier for the source camera"
    }
  },
  "required": ["timestamp", "confidence", "camera_ID"],
  "additionalProperties": false
}